---
title: "Segurança"
description: "Conteúdo consolidado na página de autenticação"
icon: "shield"
---

<Warning>
  Este conteúdo foi consolidado na página [Autenticação, mTLS e allowlist de IPs](/iniciacao/autenticacao).
</Warning>

## Camadas principais de segurança

Sua integração deve combinar:

1. **Credenciais da API**: `x-delbank-api-key` e `x-delfinance-account-id`
2. **mTLS**: certificado do cliente e chave privada em Produção
3. **Allowlist de IPs**: IPs públicos de saída aprovados em Produção

## Proteção de credenciais

<Warning>
  **Nunca** exponha chaves, certificados, chaves privadas ou tokens em:

  - repositórios públicos ou privados sem criptografia
  - logs de aplicação
  - front-end (JavaScript, mobile, apps)
  - URLs (query strings)
  - mensagens de erro exibidas ao usuário final
</Warning>

### Recomendações

- utilize **cofres de segredos**: AWS Secrets Manager, HashiCorp Vault, Google Secret Manager, Azure Key Vault;
- armazene credenciais separadas por ambiente;
- restrinja acesso a chaves privadas com permissões adequadas;
- use **HSM** (Hardware Security Module) em ambientes críticos.

## Transporte seguro

- **TLS 1.2 ou superior** obrigatório.
- **mTLS** em Produção (veja [mTLS](/iniciacao/mtls)).
- **Allowlist de IPs** em Produção (veja [Filtros de IPs](/iniciacao/filtros-ip)).
- rejeite ciphers fracos (RC4, DES, MD5).

## Validação de webhooks

Ao receber webhooks da Delfinance:

1. Valide a assinatura HMAC no header `X-Delfinance-Signature`.
2. Verifique se o timestamp está dentro de uma janela de 5 minutos.
3. Confirme a origem via allowlist de IPs.

```javascript
import crypto from 'crypto';

function validateWebhook(payload, signature, secret) {
  const expected = crypto
    .createHmac('sha256', secret)
    .update(payload)
    .digest('hex');
  return crypto.timingSafeEqual(
    Buffer.from(signature),
    Buffer.from(expected)
  );
}
```

## Rate limiting e proteção contra abuso

- respeite os limites de taxa retornados nos headers `X-RateLimit-*`;
- implemente **backoff exponencial** em caso de `429 Too Many Requests`;
- monitore anomalias de tráfego na sua aplicação.

## Dados sensíveis (LGPD)

- colete apenas os dados necessários;
- criptografe dados em repouso (AES-256) e em trânsito (TLS);
- implemente controles de acesso baseados em função (RBAC);
- documente a base legal do tratamento de dados.

## Reportando vulnerabilidades

Se você identificar uma vulnerabilidade, envie um e-mail para `security@delfinance.com.br`. Não divulgue publicamente antes da correção.